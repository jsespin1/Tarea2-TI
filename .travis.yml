language: ruby
rvm:
- 2.2.3

before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - cp config/database.yml.travis config/database.yml

  deploy:
  provider: heroku
  app:
    master: peaceful-scrubland-44962.herokuapp.com